#include<iostream>
using namespace std;
class rectangle{
	private:
		double length;
		double breadth;
		string color;
		public:
			rectangle(double len,double brd):length(len),breadth(brd),color("none"){}
};